<Page
    x:Class="Nazuna.FilesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Nazuna"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid RowDefinitions="Auto, *">
        <Grid ColumnDefinitions="Auto, Auto, Auto, *, Auto">
            <Button Margin="8, 0, 0, 0" Background="Transparent" BorderBrush="Transparent">
                <TextBlock Text="Upload" x:Uid="UploadButton" />
            </Button>
            <Button Grid.Column="1" Margin="8, 0, 0, 0" Background="Transparent" BorderBrush="Transparent">
                <TextBlock Text="Download" x:Uid="DownloadButton" />
            </Button>
            <Button Grid.Column="2" Margin="8, 0, 0, 0" Background="Transparent" BorderBrush="Transparent">
                <TextBlock Text="Delete" x:Uid="DeleteButton" />
            </Button>
            <Button Grid.Column="4" Margin="0, 0, 8, 0" Background="Transparent" BorderBrush="Transparent" Click="Logout">
                <TextBlock Text="Logout" x:Uid="LogoutButton" />
            </Button>
        </Grid>
        <ListView Grid.Row="1" Name="Files" Margin="8">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:FileInfo">
                    <Grid Margin="0, 8, 0, 8" ColumnDefinitions="*, Auto, Auto">
                        <TextBlock Text="{x:Bind FileName}" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" />
                        <TextBlock Grid.Column="1" Margin="16, 0, 0, 0" Text="{x:Bind LastModified}" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" />
                        <TextBlock Grid.Column="2" Margin="16, 0, 0, 0" Text="{x:Bind Size}" TextTrimming="CharacterEllipsis" TextWrapping="NoWrap" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>